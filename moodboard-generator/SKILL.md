---
name: moodboard-generator
description: "Create a themed HTML and PNG visual moodboard when the user wants curated visual references beyond a game emotional-canvas workflow."
---

# Moodboard Generator

Create a themed HTML and PNG visual moodboard when the user wants curated visual references beyond a game emotional-canvas workflow.

## Result

Create a themed HTML and PNG visual moodboard when the user wants curated visual references beyond a game emotional-canvas workflow.

## Use the method proportionately

Keep board JSON as the editable source. Inspect image candidates and the resulting composition. Read the detailed guide's script/schema sections before rendering. Image sourcing and browser export must use tools permitted by the host; no specific browser or web_fetch tool is assumed. If PNG export is unavailable, report that limit and return the verified HTML.

Match depth and output format to the requested decision. Use known inputs; ask for missing information only when it changes the result. A narrow request does not require a full audit or every output section.

For the full method, definitions, detailed checks and examples, consult [the detailed guide](references/detailed-guide.md), reading the sections relevant to the current question. Load its supporting references only when their specific taxonomy, schema or example is needed. Preserve concrete method and file-format requirements; numbered examples are not a required itinerary for every task.
