---
name: game-design-one-page-design-doc
description: "Create a one-page game concept document as Markdown, structured JSON and PDF when a compact design handoff is requested."
---

# Game Design One-Page Design Doc

Create a one-page game concept document as Markdown, structured JSON and PDF when a compact design handoff is requested.

## Result

- **One-page PDF** — the shareable, visually designed version
- **One-page markdown doc** — the editable text version
- **Structured source JSON** — the intermediate data used for rendering

## Use the method proportionately

Preserve the JSON field contract and produce editable Markdown plus a one-page PDF unless the user narrows the deliverable. Read the detailed guide's renderer/schema sections before creating files. Inspect page fit; the renderer is not evidence that all content fits.

Match depth and output format to the requested decision. Use known inputs; ask for missing information only when it changes the result. A narrow request does not require a full audit or every output section.

For the full method, definitions, detailed checks and examples, consult [the detailed guide](references/detailed-guide.md), reading the sections relevant to the current question. Load its supporting references only when their specific taxonomy, schema or example is needed. Preserve concrete method and file-format requirements; numbered examples are not a required itinerary for every task.
